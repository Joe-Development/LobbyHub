Config = {
    defaultLobby = 0, -- 0 = Roleplay, 1 = Training, 2 = PVP
    lobby_hud = {
        enabled = true, -- set to false if u want to use freech hud
        defaultText = "~t~Lobby: {LOBBY}",
        fontSize = 0.35,
		x = 1.400,
		y = 0.545,
    }
}